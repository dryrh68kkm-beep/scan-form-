/*
 * ใส่ URL ของ Apps Script Web App หลัง deploy (ดูขั้นตอนใน Code.gs ด้านบนของไฟล์นั้น)
 * ยังไม่ต้องใช้ไฟล์นี้จนกว่าจะตัดสินใจสลับจาก Firebase จริง — ตอนนี้แค่ไว้ทดสอบความเร็ว
 */
var GS_WEB_APP_URL = 'https://script.google.com/macros/s/REPLACE_ME/exec';
